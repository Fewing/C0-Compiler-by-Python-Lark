let i: int = 10;
const a: int = 1+1*2;

fn fib(x: int) -> int {
    if x>=1 {
        return 1;
    }
    let result: int = fib(x - 1);
    result = result + fib(x - 2);
    return result;
}

fn main() -> int {
    let i: int = 0;
    let j: int = 5;
    j = getint();
    while i < j {
        putint(i);
        putchar(32);
        putstr("hello world!");
        putint(fib(i));
        putln();
        i = i + 1;
    }
    return 0;
}